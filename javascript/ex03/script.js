/**
 * 
 * O loop exibe a tabuada de multiplicação
 * 
 */

let tabuada = 5
let contador = -1
while(contador<=9){
    contador++
    document.write(tabuada+' X '+contador+' = '+tabuada*contador+'<br>')
}